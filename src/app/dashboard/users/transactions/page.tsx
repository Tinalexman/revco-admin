import Transactions from "@/components/dashboard/users/transactions/Transactions";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Transaction History",
};

export default Transactions;
