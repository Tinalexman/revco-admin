import React, { FC } from "react";

const GenerateInvoice: FC<{ onClose: () => void }> = ({ onClose }) => {
  return <div className="w-full bg-white flex flex-col ">GenerateInvoice</div>;
};

export default GenerateInvoice;
